# git helper (run locally if the agent could not spawn a terminal)

# Example incremental history if starting from the requirements commit:
# git add docs/ARCHITECTURE.md docs/TRADEOFFS.md docs/SCOPE_CLEARANCE_EMAIL.md
# git commit -m "docs: architecture, trade-offs, and scope clearance"
#
# git add backend/pom.xml backend/src/main/java/com/acme/salary/SalaryApplication.java backend/src/main/resources/application.yml backend/src/main/resources/db/migration backend/src/main/java/com/acme/salary/config
# git commit -m "chore: Spring Boot skeleton with Flyway and health"
#
# Continue with security, employee API, insights+seed, frontend, docker.
